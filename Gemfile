# frozen_string_literal: true

source 'https://rubygems.org'

gemspec

# Required for Ruby 4.0+ (moved out of stdlib)
gem 'logger'

group :development, :test do
  gem 'rspec', '~> 3.13'
  gem 'webmock', '~> 3.26'
end

